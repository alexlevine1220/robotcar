from cv2 import cv2
import numpy as np

#img = np.zeros((100, 100, 3))
#cv2.circle(img, (50, 50), 5, (0, 0, 255))

#cv2.imshow("", img)
# cv2.waitKey()

for x in np.arange(10.0, 100, 0.1):
    print(x)
